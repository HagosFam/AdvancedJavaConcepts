package questions;

public class PasswordChecker {

    public static boolean passwordChecker(String password) {
        boolean okayLength, hasUpper, hasLower, hasDigit, hasChar  = false;


        for(int i= 0; i<password.length() -1; i++) {
            // length should be min of 8 and max of 16
           // boolean lengthChecker = lengthCheker(password.charAt(i));

            // it should have atleast one capital letter
            // it should have atleast one small letter
            // it should have atleast one digit, isNumberic
            // it should have atleast special charachter checker
        }
//      if(okayLength && hasUpper && hasLower && hasDigit && hasChar) {
//          return true;
//        }
        return true;
    }

}
